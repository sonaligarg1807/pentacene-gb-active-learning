setup notes:
- Creation of github repo:
- created folder: pentacene-gb-active-learning --> open this folder in vscode --> click on source control --> initialise repository --> create one file --> README.md --> add it and commit it --> publish repository or gut push -u origin main
- create folder structure inside this repo as follows:
pentacene-gb-active-learning/
├── README.md
├── .gitignore
├── environment.yml
├── configs/
│   └── al_config.yaml
├── data/
│   ├── seed_structures/
│   └── labeled_data/
├── src/
│   ├── __init__.py
│   ├── committee.py
│   ├── md_runner.py
│   ├── labeler.py
│   ├── transfer_integral.py
│   ├── acquisition.py
│   └── al_loop.py
├── scripts/
│   ├── run_seed_generation.py
│   └── run_al_cycle.py
├── notebooks/
│   └── analysis.ipynb
└── output/
    ├── models/
    └── results/

- then write .gitignore: This file is written to keep large model checkpoints and generated data out of git history ( can check track code, not big binary outputs )

- write a minimal readme file
- setup conda environment, bu running environment.yaml file as:
conda env create -f environment.yaml

This will create the conda environment, but it will show error in installing xtb-python via pip --> first activate the generated conda environment as `conda active pentacene-al` --> then install xtb via conda as `conda install -c conda-forge xtb-python`

- then verify whether all environments are working or not.
generate test_setup.py script in scripts/
add imports to test and run this script.
- before this make sure, python interpreter for this conda session is selected as "pentacene-al" and conda env is active

- in my case, it gave error for mace-torch. so install it as `pip install mace-torch"

- try rerunning the test_setup.py script again. it said everuthing ok.
- stage the commits till here.

# Step 2: Seed Dataset Generation
- Generated 304 pentacene dimer geometries (19 angles x 4 axes x 4 separations,
  with random slip and thermal jitter), zero skipped combinations.
- Verified axis vectors against ASE's own .cif parsing.
- Output: `data/seed_structures/generated_dimers/` (304 .xyz files),
  `seed_dimers_manifest.csv`.
- Jitter is applied to perturb the structures to mimic thermal fluctuations

- generated configuration file in config/seed_generation.yaml
- script for helper functions for dimer generation: src/dimer_utils.py
- script for running dimer generation: scripts/generate_seed_dimers.py
- output files: generated_dimers --> 304 structures and a csv file
- notebooks: analysis.ipynb

- points to keep in mind: used monomer.xyz structure from pen-schellhammer, cif file from this folder to get unit cell vectors
- Verified thermal jitter (std=0.05 A) is correctly applied: measured bond-length
  fluctuations (C-C std=0.068 A, C-H std=0.052 A) match theoretical expectation
  (sigma_bond ~ sigma_atom x sqrt(2)), confirming realistic, non-destructive
  perturbation.

## step3: Running a GFN2-xTB single point calculations on each dimer structures to get ground truth energy and forces.
- added following files: config/labelling_config.yaml; src/labeler.py; scripts/label_seed_dimers.py
- Ground-Truth Labeling — Complete
- Ran GFN2-xTB single-point calculations on all 304 seed dimer structures.
- 304/304 succeeded, 0 failures.
- Output: `data/labeled_data/seed_dataset_labeled.xyz` (MACE-ready extended XYZ,
  with energy in eV and forces in eV/Angstrom embedded per structure/atom).

## step4(a): Training a small MACE model from scratch on 304 dimer labeled structures 
- first run failed because of wrong keys used for forces and energies; there is no reference atomic energies (EOs) data
- why EOs are needed: because instead of learning absolute total energies directly, the model learns the binding/interaction energy relative to isolated atoms.
- fixing the missing data and keys

- computing atomic energies from xTB. since pentacene has C and H atoms only, use `scripts/compute_atomic_energies.py` and it will print the atomic energies which we have to insert in mace config file
printed atomic energies `E0s`
H: E0 = -10.707211 eV
C: E0 = -48.847445 eV

- after fixing key names and adding E0s --> training is running in tmux window
- Fixed E0s argument (typo: EOs -> E0s) and dictionary formatting.
- Fixed energy/forces key naming (renamed to REF_energy/REF_forces per
  MACE's ASE 3.23+ compatibility recommendation).
- Training running successfully: loss decreasing from 28.96 -> 1.79 within
  first 2 epochs (200 epochs total planned).
- Output: `output/models/scratch_v1/` (checkpoints, logs)

-step4a --> completed
stopped the model training manually at epoch 126 due to: 1. over heating of laptop as running locally on CPUs --> so shifting everything on Justus2
2. Model is degrading and overfitting. from the log it already seems that the model is degreding but still will be using the best epoch checkpoint file as best model.

--step4b: shifted everything to Justus2
- there cloned the repository
- created conda environment; installed mace-torch and xtb-python.
- had some problem with PYTHONPATH set already so used this:
module purge
unset PYTHONPATH
conda activate pentacene-al
python -c "import torch; print(torch.__file__); 

then downloaded the mace foundation model as:
python -c "
from mace.calculators import mace_off
calc = mace_off(model='medium', return_raw_model=True)
"

this will download the mace foundation model which we will use to retrain our Dataset.
to find the exact path of the foundation model to be added in the config yaml: find ~/.cache/mace -name "*.model" -o -name "*.pt"

- created the following files: configs/mace_finetune_config.yaml
add proper paths and path to downloaded foundation model

- then added wrapper script as: scripts/train_finetune_model.py
no changes needed in that

- added a slurm submission script to run with GPUs: scripts/finetune_justus2.slurm
added conda environment

- although Justus2 has been fixed but training couldn't run because it is down

- so tried on colab but that doesn't work as well. it was lot of setup work. changing runtime type from CPUs to GPUs --> mounting drive --> installing mace-torch and cloning git repo. also dataset was not present because t was quite large so not uploaded on git repo.

- now trying on TCB
